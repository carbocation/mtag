[pytest]
testpaths = tests ldsc_mod/test
markers =
    slow: long-running statistical regression tests
filterwarnings =
    ignore:the matrix subclass is not the recommended way:PendingDeprecationWarning
